pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Local NDI AAR resolved from app/libs/
        flatDir { dirs("app/libs") }
    }
}

rootProject.name = "NDIWebcam"
include(":app")
